#include <stdio.h>
#include "main.h"
/**
 * main - Entry Point
 * @argc: arguments count
 * @argv: argument vector (1-dimension array of string)
 * Return: 0
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n, argv[0]");
	return (0);
}
